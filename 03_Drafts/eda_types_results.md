# EDA Types Results — wildcat_loans_clean.csv

Generated by `scripts/eda_types.py`.

## origination_date Type Conversion

| Stage | Type |
|---|---|
| Before | object (text/str) |
| After | datetime64[us] |

Conversion successful — `origination_date` now supports date arithmetic, filtering, and month/year extraction. (Note: this pandas version stores datetime at microsecond resolution, `datetime64[us]`, rather than the `datetime64[ns]` shown in the tutorial — functionally the same datetime type.)

All other columns matched their expected types from Step 1 and required no conversion.
